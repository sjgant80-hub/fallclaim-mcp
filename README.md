# fallclaim-mcp

MCP stdio server wrapping `@ai-native-solutions/fallclaim-sdk`.

## Install
```bash
claude mcp add fallclaim -- npx -y @ai-native-solutions/fallclaim-mcp
```

MIT · AI-Native Solutions
